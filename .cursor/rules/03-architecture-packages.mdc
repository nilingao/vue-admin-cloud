---
alwaysApply: true
description: 项目架构和包管理规则
---

# 项目架构和包管理

## Monorepo 架构

项目采用 Monorepo 架构，使用 pnpm workspace + Turbo 进行管理：

- **工作区配置**: [pnpm-workspace.yaml](mdc:pnpm-workspace.yaml)
- **构建配置**: [turbo.json](mdc:turbo.json)
- **包管理**: 使用 pnpm catalog 统一管理依赖版本

## 包结构说明

### 核心包 (@core)

#### @core/base
- **design**: 设计系统相关
- **icons**: 图标组件
- **shared**: 共享工具和类型
- **typings**: 类型定义

#### @core/ui-kit
- **form-ui**: 表单组件
- **layout-ui**: 布局组件
- **menu-ui**: 菜单组件
- **popup-ui**: 弹窗组件
- **shadcn-ui**: shadcn/ui 组件
- **tabs-ui**: 标签页组件

#### @core/composables
- 组合式 API 函数

#### @core/preferences
- 偏好设置管理

### 功能包 (effects)

#### @vben/access
- 权限访问控制

#### @vben/common-ui
- 通用 UI 组件

#### @vben/hooks
- 自定义 Hooks

#### @vben/layouts
- 布局组件

#### @vben/plugins
- 插件系统

#### @vben/request
- HTTP 请求封装

### 工具包

#### @vben/constants
- 常量定义

#### @vben/icons
- 图标库

#### @vben/locales
- 国际化

#### @vben/preferences
- 偏好设置

#### @vben/stores
- 状态管理

#### @vben/styles
- 样式系统

#### @vben/types
- 类型定义

#### @vben/utils
- 工具函数

## 应用结构

### web-antd 应用
- **入口文件**: [apps/web-antd/src/main.ts](mdc:apps/web-antd/src/main.ts)
- **应用配置**: [apps/web-antd/src/app.vue](mdc:apps/web-antd/src/app.vue)
- **路由配置**: [apps/web-antd/src/router/](mdc:apps/web-antd/src/router/)
- **页面组件**: [apps/web-antd/src/views/](mdc:apps/web-antd/src/views/)
- **API 接口**: [apps/web-antd/src/api/](mdc:apps/web-antd/src/api/)

## 依赖管理

### Catalog 依赖
项目使用 pnpm catalog 统一管理依赖版本，主要依赖包括：

- **Vue 生态**: vue, vue-router, pinia
- **构建工具**: vite, typescript, unbuild
- **UI 库**: ant-design-vue, element-plus, naive-ui
- **工具库**: @vueuse/core, dayjs, axios
- **开发工具**: eslint, prettier, stylelint

### 工作区依赖
- 使用 `workspace:*` 引用本地包
- 使用 `catalog:` 引用统一版本管理

## 构建配置

### Vite 配置
- **主配置**: [apps/web-antd/vite.config.mts](mdc:apps/web-antd/vite.config.mts)
- **通用配置**: [internal/vite-config/](mdc:internal/vite-config/)

### TypeScript 配置
- **应用配置**: [apps/web-antd/tsconfig.json](mdc:apps/web-antd/tsconfig.json)
- **通用配置**: [internal/tsconfig/](mdc:internal/tsconfig/)

### Tailwind 配置
- **应用配置**: [apps/web-antd/tailwind.config.mjs](mdc:apps/web-antd/tailwind.config.mjs)
- **通用配置**: [internal/tailwind-config/](mdc:internal/tailwind-config/)

## 开发工作流

### 本地开发
```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 启动特定应用
pnpm dev:antd
```

### 构建部署
```bash
# 构建所有包
pnpm build

# 构建特定应用
pnpm build:antd

# 构建分析
pnpm build:analyze
```

### 代码质量
```bash
# 代码检查
pnpm lint

# 代码格式化
pnpm format

# 类型检查
pnpm check:type

# 全面检查
pnpm check
```