---
alwaysApply: true
description: Vue Vben Admin 项目概览和核心信息
---

# Vue Vben Admin 项目概览

这是一个基于 Vue Vben Admin 的现代化中后台管理系统模板项目。

## 项目基本信息

- **项目名称**: Vue Vben Admin (vben-admin-monorepo)
- **版本**: 5.5.9
- **技术栈**: Vue 3 + Vite + TypeScript + Ant Design Vue
- **包管理器**: pnpm (>=9.12.0)
- **Node.js**: >=20.10.0
- **许可证**: MIT

## 核心配置文件

- [package.json](mdc:package.json) - 项目依赖和脚本配置
- [turbo.json](mdc:turbo.json) - Turbo 构建配置
- [pnpm-workspace.yaml](mdc:pnpm-workspace.yaml) - pnpm 工作区配置
- [vitest.config.ts](mdc:vitest.config.ts) - Vitest 测试配置

## 主要目录结构

```bash
├── apps/                    # 应用目录
│   └── web-antd/           # 基于 Ant Design Vue 的前端应用
├── packages/               # 包目录
│   ├── @core/             # 核心包
│   │   ├── base/          # 基础包 (design, icons, shared, typings)
│   │   ├── composables/   # 组合式 API
│   │   ├── preferences/   # 偏好设置
│   │   └── ui-kit/        # UI 组件集合
│   ├── effects/           # 副作用相关包
│   │   ├── access/        # 访问控制
│   │   ├── common-ui/     # 通用 UI
│   │   ├── hooks/         # 组合式 API
│   │   ├── layouts/       # 布局
│   │   ├── plugins/       # 插件
│   │   └── request/       # 请求
│   ├── constants/         # 常量
│   ├── icons/             # 图标
│   ├── locales/           # 国际化
│   ├── preferences/       # 偏好设置
│   ├── stores/            # 状态管理
│   ├── styles/            # 样式
│   ├── types/             # 类型定义
│   └── utils/             # 工具
├── internal/              # 内部工具
│   ├── lint-configs/      # 代码检查配置
│   ├── node-utils/        # Node.js 工具
│   ├── tailwind-config/   # Tailwind 配置
│   ├── tsconfig/          # TypeScript 配置
│   └── vite-config/       # Vite 配置
└── scripts/               # 脚本目录
```

## 开发命令

- `pnpm install` - 安装依赖
- `pnpm dev` - 开发环境启动
- `pnpm dev:antd` - 启动 Ant Design Vue 应用
- `pnpm build` - 构建生产版本
- `pnpm build:antd` - 构建 Ant Design Vue 应用
- `pnpm lint` - 代码检查
- `pnpm format` - 代码格式化
- `pnpm test:unit` - 单元测试
- `pnpm check` - 全面检查（循环依赖、类型、拼写等）

## 主要特性

- **Monorepo 架构**: 使用 pnpm workspace + Turbo 管理多包项目
- **现代化技术栈**: Vue 3 + Vite + TypeScript + Ant Design Vue
- **组件化设计**: 高度模块化的组件系统
- **国际化支持**: 内置多语言支持
- **主题系统**: 支持多主题和暗色模式
- **权限管理**: 完整的权限控制系统
- **代码质量**: ESLint + Prettier + Stylelint + TypeScript 严格模式
